const conf = {
    appwriteUrl: String(import.meta.env.VITE_APPWRITE_URL),
    appwriteDatabaseId: String(import.meta.env.VITE_PROJECT_ID),
    appwriteCollection: String(import.meta.env.VITE_APPWRITE_URL),
    appwriteBucket: String(import.meta.env.VITE_APPWRITE_URL),
    appwriteProjectId: String(import.meta.env.VITE_APPWRITE_URL),
}

export default conf